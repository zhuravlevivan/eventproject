version: '3.8'

services:
  app:
    image: django
    command: python manage.py runserver 0.0.0.0:7000
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: django
